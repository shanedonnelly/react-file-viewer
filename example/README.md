This is a Next TS mini app to test the react-file-viewer package.

## Try yourself

```bash
cd example
npm install
npm run dev
```

Then open http://localhost:3000 in your browser to see the file viewer in action.